# Source-neutral content

This example builds immutable structured content, projects its semantic text,
and validates explicit resource limits without selecting terminal presentation

Run it from the Rust repository root:

```sh
cargo run -p nagi-content --example content
```

It prints two semantic text lines followed by `nodes=5 depth=3`
